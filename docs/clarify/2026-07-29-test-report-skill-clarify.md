# 需求澄清结论 — 测试报告生成 Skill（2.0 T3）

> 阶段：clarify（需求澄清）
> 技能：/brainstorming
> 日期：2026-07-29
> 状态：✅ 已澄清（全自动流水线模式，开放问题已静默接管决策）

---

## 一、项目上下文探查结论（SSOT 已验证事实）

| 维度 | 事实 | 证据 |
|------|------|------|
| 技术栈 | Taro 4.2.0 + React 18 + TypeScript + Vite | `package.json` templateInfo / deps |
| 项目类型 | 记账小程序（iMoney），小程序工程 | `package.json` description="记账小程序" |
| **测试框架现状** | **当前无任何测试框架与 test 脚本** | `package.json` 无 `scripts.test`，无 jest/vitest 依赖 |
| 仓库历史 | auto-dev 流水线（已有 clarify→plan→coding 阶段提交） | `git log` |
| 输出位置惯例 | 项目存在 `docs/` 目录 | 目录结构 |

---

## 二、开放问题澄清决策（Q1/Q2/Q3）

按防阻塞协议，以下 3 个开放问题已依据"上下文优先 → 安全兜底 → 行业最佳实践"自动决策。

### Q1：首期目标项目栈是否以 TypeScript/Node 为主？

**决策：是，首期以 TypeScript/Node 为 P0 主栈。**

依据：
- 上下文优先：当前宿主仓库（iMoney）即为 TypeScript/Node（Taro+React+TS）工程，直接验证了 Q1 假设；
- 需求文档已按此假设制定 P0 范围，决策一致；
- 通用 JUnit XML 作为跨语言兜底格式，保证未来向 Go/Rust 扩展时不受影响。

**衍生澄清点**：当前 iMoney 仓库**尚未引入测试框架**。这意味着 FR1.1 的框架自动识别在真实宿主仓库将命中"识别失败"分支。需明确补强：

> **澄清补充 C1（重要）**：FR1.1 框架识别优先级链路末端须增加**"识别失败 → 给出明确诊断 + 引导用户显式指定 test_command 或切换解析模式"**的降级路径，不得静默生成空报告。该行为与 FR1.4 一致，应作为兜底分支显式实现。

### Q2：报告是否需要中/英双语模板，还是仅中文？

**决策：首期仅中文模板；模板渲染层预留语言参数，双语列为 M4+ 迭代候选。**

依据：
- 安全兜底 + 最小改动：仅中文降低首期复杂度与测试样本量；
- 需求文档用户故事/验收标准均为中文语境；
- 行业最佳实践：模板渲染层与文案解耦，通过 locale 参数控制，避免未来加双语时重写。

### Q3：是否需要将报告自动推送到 IM / 邮件等渠道？

**决策：本期不做，维持"非目标"列定，不澄清变更。**

依据：
- 需求 2.2 已将其明确列为非目标；
- 推送渠道涉及凭证/网络外部副作用，与 NFR3（安全）边界冲突风险高，本期规避。

---

## 三、需求完整性评估

需求描述已具备交付级完整度，无需澄清补充的核心字段如下表：

| 需求板块 | 完整性 | 备注 |
|----------|--------|------|
| 背景与问题 | ✅ 完整 | — |
| 目标 / 非目标 | ✅ 完整 | 边界清晰 |
| 用户故事 US1-US4 | ✅ 完整 | 含验收要点 |
| FR1-FR4 功能需求 | ✅ 完整 | 含双模式、插件式解析 |
| NFR1-NFR5 非功能需求 | ✅ 完整 | 含性能/健壮性/安全/幂等/可维护 |
| AC1-AC5 验收标准 | ✅ 完整 | 可验证 |
| M1-M4 里程碑 | ✅ 完整 | P0/P1/P2 分级明确 |

---

## 四、设计阶段待解决的方案选项（移交下一阶段）

以下不属澄清范畴，作为 brainstorming"Propose approaches"阶段的输入项登记，不在本 clarify 产物中展开：

1. **解析器插件式架构的具体形态**（NFR5）：
   - 选项 A：统一 `TestResultParser` 接口 + 各框架独立实现类 + 注册表查找；
   - 选项 B：基于 schema 映射表的声明式解析（配置驱动）；
   - 选项 C：管道式（按格式嗅探 → 适配 → 归一化）。
   → 移交设计阶段提案并推荐 A（隔离性最佳，符合 brainstorming "Design for isolation"）。
2. **长任务执行与后台轮询机制**（R2）：依赖 Agent 运行时后台任务能力，设计阶段需确认是否复用 `run_in_background` + 轮询。
3. **覆盖率数据来源**：Jest/Vitest 的 coverage 目录 vs istanbul json-summary，需在解析器实现中统一。

---

## 五、澄清结论与流转

**澄清结论**：
- Q1 ✅ 已决策（TS/Node 为 P0）
- Q2 ✅ 已决策（仅中文，预留 locale）
- Q3 ✅ 已决策（本期不做）
- C1 补充 ✅（FR1.1 须补强"识别失败降级诊断"分支）

**无信息缺失阻塞项**，需求描述已达可设计完整度。

**流转方向**：clarify 阶段完成 → 进入 brainstorming 后续步骤（Propose approaches → Present design → Write design doc → Spec self-review）。设计文档将落盘至 `docs/specs/`。
